# Binance Top 50 Perpetual Base Universe
base_preset: presets/crypto_cex_preset_binance_top50_perp.yaml

# No strategy filters, just base universe
trend:
  recommendation: {enabled: false}
  adx: {enabled: false}
  momentum: {enabled: false}

export_metadata:
  symbol: binance_top50_perp_base
  data_category: universe_selector
