# Futures Trend Momentum (Long) - Refactored
base_preset: presets/futures_us_liquid.yaml

trend:
  direction: "long"
  timeframe: "daily"
  logic: "AND"
  recommendation:
    enabled: true
    min: 0.4
  adx:
    enabled: true
    min: 25
  momentum:
    enabled: true
    horizons: {change: 0.0, Perf.W: 0.5, Perf.1M: 3.0}
  confirmation_momentum:
    enabled: true
    horizons: {Perf.3M: 5.0}

export_metadata:
  symbol: "futures_trend_momentum"
  data_category: "universe_selector"