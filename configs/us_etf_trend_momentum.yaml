# US ETFs trend momentum (daily) layered on base
base_preset: us_etf_base_universe.yaml

limit: 250
prefilter_limit: 3000
pagination_size: 200
sort_by: market_cap_basic
final_sort_by: volume

trend:
  direction: "long"
  timeframe: "daily"
  logic: "AND"
  recommendation:
    enabled: false
  adx:
    enabled: true
    min: 8
  momentum:
    enabled: true
    horizons: {change: 0.0, Perf.W: 0.2, Perf.1M: 1.0, Perf.3M: 2.0}
  confirmation_momentum:
    enabled: false

export_metadata:
  symbol: "us_etf_trend_momentum"
  data_category: "universe_selector"
