# Forex Trend Momentum (Long) - Refactored
base_preset: presets/forex_liquid.yaml

trend:
  direction: "long"
  timeframe: "daily"
  logic: "AND"
  recommendation:
    enabled: true
    min: 0.3
  adx:
    enabled: true
    min: 20
  momentum:
    enabled: true
    horizons: {change: 0.0, Perf.W: 0.5, Perf.1M: 1.0}

export_metadata:
  symbol: "forex_trend_momentum"
  data_category: "universe_selector"