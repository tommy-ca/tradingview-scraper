# US Stocks Trend Momentum (Long) - Refactored
base_preset: presets/stocks_us_liquid.yaml

limit: 250 # Override base limit for broader scan

trend:
  direction: "long"
  timeframe: "daily"
  logic: "AND"
  recommendation:
    enabled: true
    min: 0.4
  adx:
    enabled: true
    min: 20
  momentum:
    enabled: true
    horizons: {change: 0.0, Perf.W: 1.0, Perf.1M: 5.0}
  confirmation_momentum:
    enabled: true
    horizons: {Perf.1M: 5.0, Perf.3M: 10.0}

export_metadata:
  symbol: "us_stocks_trend_momentum"
  data_category: "universe_selector"