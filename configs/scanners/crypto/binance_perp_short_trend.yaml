# L4: Binance Perp Alpha Trend (Short Only)
base_preset: ../../base/universes/binance_perp_top50.yaml
trend:
  direction: "short"
  logic: "AND"
  adx: {enabled: true, min: 10}
  recommendation: {enabled: true, min: -0.1}
  momentum:
    enabled: true
    horizons: {Perf.W: 0.0, Perf.1M: 0.0}
limit: 50
export_metadata:
  symbol: binance_perp_short
  data_category: strategy_alpha
